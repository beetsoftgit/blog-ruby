<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">Shimidzu</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Link</a>
      </li>
    </ul>
    <%= form_tag(posts_path, method: :get, class: 'form-inline my-2 my-lg-0') do %>
        <%= text_field_tag :term, params[:term], class: 'form-control mr-sm-2', placeholder: 'Input text to search' %>
        <%= submit_tag 'Search', name: nil, class: 'btn btn-outline-success my-2 my-sm-0' %>
    <% end %>
  </div>
  <% if current_user %>
    <ul class="nav nav-tabs">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
          <%= image_tag('account.png', size: '40x40', alt: 'Account') %>
        </a>
        <div class="dropdown-menu">
          <%= link_to('Logout', logout_path, method: :delete, class: 'dropdown-item') %>
          <%= link_to('Profile', user_path(current_user), class: 'dropdown-item') %>
        </div>
      </li>
    </ul>
  <% else %>
      <ul class="navbar-nav mr-auto">
        <li class="nav-link">
          <a class="btn btn-primary" href="/login" role="button">Login</a>
        </li>
      </ul>
  <% end %>
</nav>